---
phase: 05-pr-ci-workflow-linux-validation-path
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/security-ci.yml
  - .planning/phases/05-pr-ci-workflow-linux-validation-path/VERIFICATION.md
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
  - CI-04
user_setup: []
must_haves:
  truths:
    - Opening a PR to main triggers a workflow that runs ./scripts/validate-security.sh on ubuntu-latest.
    - Workflow uses permissions contents read only and named checkout + validate steps.
  artifacts:
    - .github/workflows/security-ci.yml exists and matches CONTEXT decisions.
    - VERIFICATION.md records evidence for Phase 5.
  key_links:
    - validate-security.sh remains the single validation entrypoint locally and in CI.
---

<objective>
Add GitHub Actions workflow `security-ci.yml` that runs `./scripts/validate-security.sh` on every pull request targeting `main`, with least-privilege permissions and clear step names for CI-01–CI-04.

Purpose: Automate the existing local security gate on Linux runners without expanding outbound surface.

Output: Workflow YAML + Phase 5 VERIFICATION.md.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pr-ci-workflow-linux-validation-path/05-CONTEXT.md
@scripts/validate-security.sh

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PR security validation workflow</name>
  <files>.github/workflows/security-ci.yml</files>
  <read_first>.planning/phases/05-pr-ci-workflow-linux-validation-path/05-CONTEXT.md,docs/security/outbound-allowlist.md</read_first>
  <action>Create `.github/workflows/security-ci.yml` with: `name` identifying security validation; `on.pull_request.branches: [main]`; top-level `permissions: contents: read`; optional `concurrency` group `ci-${{ github.workflow }}-${{ github.ref }}` with cancel-in-progress true; job id `security-validate`, `runs-on: ubuntu-latest`, steps (1) actions/checkout@v4 with clear name, (2) run `./scripts/validate-security.sh` from repo root with step title matching CI-04 (log separation). Do not add secrets, third-party actions beyond checkout, or workflow_dispatch.</action>
  <verify>yamllint or python -c yaml parse if available; at minimum read file back</verify>
  <acceptance_criteria>
    - File path is exactly `.github/workflows/security-ci.yml`
    - Contains pull_request, branches main, permissions contents read, job security-validate, checkout@v4, run validate-security.sh
  </acceptance_criteria>
  <done>YAML exists and matches CONTEXT D-01–D-08</done>
</task>

<task type="auto">
  <name>Task 2: Local parity check</name>
  <files>scripts/validate-security.sh</files>
  <read_first>scripts/validate-security.sh</read_first>
  <action>From repo root run `./scripts/validate-security.sh` and capture exit code 0 on clean tree.</action>
  <verify>echo exit code</verify>
  <acceptance_criteria>
    - Script exits 0 in this workspace
  </acceptance_criteria>
  <done>Exit code 0</done>
</task>

<task type="auto">
  <name>Task 3: Phase verification doc</name>
  <files>.planning/phases/05-pr-ci-workflow-linux-validation-path/VERIFICATION.md</files>
  <read_first>.planning/phases/05-pr-ci-workflow-linux-validation-path/05-CONTEXT.md</read_first>
  <action>Write VERIFICATION.md summarizing CI-01–CI-04 checks, commands run, and how PR workflow maps to decisions.</action>
  <verify>file exists with PASS/FAIL section</verify>
  <acceptance_criteria>
    - References workflow path and requirements IDs
  </acceptance_criteria>
  <done>VERIFICATION.md complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/validate-security.sh` passes locally
- [ ] Workflow YAML present and syntactically valid
</verification>

<success_criteria>
- All tasks completed
- CI-01–CI-04 traceable to files
</success_criteria>

<output>
After completion, create `05-01-SUMMARY.md` in this phase directory.
</output>
