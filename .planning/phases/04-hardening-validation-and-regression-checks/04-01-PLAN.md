---
phase: 4
title: Validation driver, regression probe, checklist, doc integration
requirements:
  - FR-3
  - FR-4
  - NFR-1
  - NFR-2
  - NFR-3
depends_on:
  - phase: "02"
  - phase: "03"
files_modified:
  - scripts/validate-security.sh
  - scripts/security-regression.sh
  - docs/security/validation-checklist.md
  - README.md
  - .planning/PROJECT.md
  - .planning/phases/04-hardening-validation-and-regression-checks/VERIFICATION.md
must_haves:
  truths:
    - "./scripts/validate-security.sh exits 0 on clean tree and non-zero if a control regresses."
    - "./scripts/security-regression.sh proves pre-commit-guard still blocks docs-vault staging."
    - "Checklist and README point maintainers to both scripts."
  artifacts:
    - "docs/security/validation-checklist.md exists."
    - "Both scripts are executable from repo root."
---

# Plan 04-01: Hardening validation and regression

## Objective

Ship Phase 4 per `04-CONTEXT.md` **D-01–D-06**: aggregate **`scripts/validate-security.sh`**, **`scripts/security-regression.sh`**, **`docs/security/validation-checklist.md`**, README + PROJECT updates, and **`VERIFICATION.md`**.

## Context

@.planning/phases/04-hardening-validation-and-regression-checks/04-CONTEXT.md  
@.planning/phases/04-hardening-validation-and-regression-checks/04-RESEARCH.md  
@.planning/ROADMAP.md  

---

## Task 1 — `validate-security.sh`

**Action:** Implement driver: `find` all `*.sh` under `scripts/` and `skills/code-to-docs-hooks/hooks/`; `bash -n` each; `py_compile` redact helper; redact smoke; require policy docs; run unsafe `CODE_TO_DOCS_VAULT` against `setup.sh` and expect failure.

**Verification:** `./scripts/validate-security.sh` → 0.

---

## Task 2 — `security-regression.sh`

**Action:** Temp git repo, stage `docs-vault/.regression-fixture`, run `pre-commit-guard.sh`, require exit 1, clean up `mktemp` dir.

**Verification:** `./scripts/security-regression.sh` → 0.

---

## Task 3 — Checklist + product docs

**Action:** Write `docs/security/validation-checklist.md`; add README section; extend `.planning/PROJECT.md` success criteria with one bullet referencing both scripts and checklist.

---

## Task 4 — Phase verification artifact

**Action:** Author `VERIFICATION.md` mapping FR/NFR and recording command appendix.

**Order:** Tasks 1–3 then 4.
